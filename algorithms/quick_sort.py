def quick_sort(arr):n    """クイックソートの実装"""n    if len(arr) <= 1:n        return arrn    n    pivot = arr[len(arr) // 2]n    left = [x for x in arr if x < pivot]n    middle = [x for x in arr if x == pivot]n    right = [x for x in arr if x > pivot]n    n    return quick_sort(left) + middle + quick_sort(right)nndef main():n    print("クイックソートプログラムへようこそ！")n    n    # サンプルデータn    sample_data = [64, 34, 25, 12, 22, 11, 90]n    print(f"元の配列: {sample_data}")n    n    sorted_data = quick_sort(sample_data)n    print(f"ソート後: {sorted_data}")n    n    # ユーザー入力でも試せるn    try:n        user_input = input("\nカンマ区切りで数値を入力してください (例: 5,3,8,1): ")n        if user_input.strip():n            user_numbers = [int(x.strip()) for x in user_input.split(",")]n            print(f"元の配列: {user_numbers}")n            sorted_user = quick_sort(user_numbers)n            print(f"ソート後: {sorted_user}")n    except ValueError:n        print("無効な入力です")nnif __name__ == "__main__":n    main()