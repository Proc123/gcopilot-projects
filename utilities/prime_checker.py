def is_prime(n):n    """数値が素数かどうかを判定する"""n    if n < 2:n        return Falsen    if n == 2:n        return Truen    if n % 2 == 0:n        return Falsen    n    for i in range(3, int(n**0.5) + 1, 2):n        if n % i == 0:n            return Falsen    return Truenndef main():n    print("素数判定プログラムへようこそ！")n    n    try:n        number = int(input("判定したい数値を入力してください: "))n        n        if is_prime(number):n            print(f"{number} は素数です")n        else:n            print(f"{number} は素数ではありません")n            n        # 素因数分解も表示n        if number > 1:n            factors = []n            temp = numbern            divisor = 2n            n            while temp > 1:n                while temp % divisor == 0:n                    factors.append(divisor)n                    temp //= divisorn                divisor += 1n                if divisor * divisor > temp:n                    if temp > 1:n                        factors.append(temp)n                    breakn            n            if len(factors) > 1:n                print(f"素因数分解: {number} = {' × '.join(map(str, factors))}")n                n    except ValueError:n        print("無効な入力です")nnif __name__ == "__main__":n    main()