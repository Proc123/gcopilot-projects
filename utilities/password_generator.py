import randomnimport stringnndef generate_password(length=12, use_letters=True, use_numbers=True, use_symbols=True):n    """パスワードを生成する"""n    characters = ""n    n    if use_letters:n        characters += string.ascii_lettersn    if use_numbers:n        characters += string.digitsn    if use_symbols:n        characters += string.punctuationn    n    if not characters:n        return "文字が選択されていません"n    n    password = "".join(random.choice(characters) for _ in range(length))n    return passwordnndef main():n    print("パスワード生成プログラムへようこそ！")n    n    try:n        length = int(input("パスワードの長さを入力してください (デフォルト: 12): ") or "12")n        n        print("\n文字の種類を選択してください:")n        use_letters = input("アルファベットを含める? (y/n, デフォルト: y): ").lower() != "n"n        use_numbers = input("数字を含める? (y/n, デフォルト: y): ").lower() != "n"n        use_symbols = input("記号を含める? (y/n, デフォルト: y): ").lower() != "n"n        n        password = generate_password(length, use_letters, use_numbers, use_symbols)n        print(f"\n生成されたパスワード: {password}")n        n    except ValueError:n        print("無効な入力です")nnif __name__ == "__main__":n    main()